server:
  port: ${SERVER_PORT:8080}
  servlet:
    contextPath: /api-citel-software

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/blood-donors-database
    username: root
    password: cetil-test123
    driver-class-name: com.mysql.cj.jdbc.Driver
    jpa:
      show-sql: true
      generate-ddl: true
      hibernate:
#        ddl-auto: update
      properties:
        hibernate:
          dialect: org.hibernate.dialect.MySQL8Dialect
          format_sql: true

